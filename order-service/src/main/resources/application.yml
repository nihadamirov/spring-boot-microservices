server:
  port: 8083

spring:
  application:
    name: order-service

  data:
    mongodb:
      uri: mongodb://rootuser:rootpass@localhost:27017/orderdb
      authentication-database: admin
      username: rootuser
      password: rootpass
      database: orderdb
      port: 27017
      host: localhost
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true